nodes: 
  - Keyboad:
      id: kb
      address: /keyboad/
  - Sin:
      id: sin
  - Master: 
      id: master

links:
  - from: { node: kb ,  output: frequency }
    to:   { node: sin,  input:  frequency }

  - from: { node: sin ,  output:   signal }
    to:   { node: master,  input:  input }

